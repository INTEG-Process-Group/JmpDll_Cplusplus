This is a C++ implementation of the JMP protocol for the INTEG JNIOR.  The JMP protocol is JSON based and replaces the binary JNIOR protocol.  This version of the DLL is built for Windows but a Cross Platform version will come.

You can learn about the JMP Protocol here, https://jnior.com/download/jmp-protocol-manual
